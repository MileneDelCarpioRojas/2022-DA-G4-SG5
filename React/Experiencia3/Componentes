import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
function Alianza(){

  const element = (
    <div>
      <h1>Arriba Alianza</h1>
      <h1>La hora local es: {new Date().toLocaleTimeString()}</h1>

    </div>

  );


  root.render(element);
}

setInterval(Alianza,1000);
